stages:
  - validate
  - build

validate-job:
  stage: validate
  image: node:20-alpine

  script:
    - node --version
    - npm --version
    - echo "Validation passed"

build-job:
  stage: build
  image: node:20-alpine

  script:
    - cd app
    - npm ci
    - echo "Application build completed"
